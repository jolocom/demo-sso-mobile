import { Dispatch, AnyAction } from 'redux'

export const startSSO = () => {
  return (dispatch: Dispatch<AnyAction>) => {
    // send credential request to the smartwallet
  }
}

export const handleJWTResponse = (params: string) => {
  return (dispatch: Dispatch<AnyAction>) => {
  }
}