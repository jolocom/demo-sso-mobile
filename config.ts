export default {
  // NOTE: the URI prefix should match:
  // intent filter scheme in android/app/src/main/AndroidManifest.xml
  // and also iOS URI schemes (from xcode)
  APP_URI_PREFIX: 'demosso://',
  BACKEND_URL: 'http://localhost:9000',
  //BACKEND_URL: 'https://demo-sso.jolocom.com',
  CONSENT_DEEPLINK: 'jolocomwallet://consent'
}
